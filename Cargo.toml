[workspace]
members = ["crates/dekopon-console", "crates/dekopon-tui"]
resolver = "3"

[workspace.package]
edition = "2024"
rust-version = "1.98.1"
license = "MIT OR Apache-2.0"
repository = "https://github.com/dekopon-agents/dekopon-console"
homepage = "https://github.com/dekopon-agents/dekopon-console"
authors = ["Dekopon contributors"]

[workspace.dependencies]
clap = { version = "4.6.4", features = ["derive", "env"] }
crossterm = { version = "0.29.0", features = ["event-stream"] }
futures-util = "0.3.31"
ratatui = { version = "0.30.0", default-features = false, features = ["crossterm", "unstable-rendered-line-info"] }
rustix = { version = "1.1.4", features = ["fs", "process"] }
serde_json = "1.0.151"
tempfile = "3.27.0"
thiserror = "2.0.19"
tokio = { version = "1.49.0", features = ["io-util", "macros", "net", "rt-multi-thread", "sync", "time", "signal"] }
tracing = { version = "0.1.44", default-features = false, features = ["std"] }

# Exact crates.io versions of one Dekopon release, never path or git dependencies.
#
# Exact =0.22.0 pins: these pre-1.0 crates and the broker wire must be tested as a set.
# Not a git dependency either — `dekopon-provider-gh` pinned a branch once, and a `branch =`
# dependency resolves by fetching the ref, so a deleted branch broke every cold-cache build.
dekopon-agent = "=0.22.0"
dekopon-broker-protocol = "=0.22.0"
dekopon-config = "=0.22.0"
dekopon-core = { version = "=0.22.0", default-features = false }
dekopon-model = "=0.22.0"
dekopon-protocol = { version = "=0.22.0", default-features = false }
dekopon-process = "=0.22.0"
dekopon-shell = "=0.22.0"
dekopon-telemetry = "=0.22.0"
opentelemetry_sdk = { version = "0.33.0", default-features = false, features = ["trace", "logs"] }
opentelemetry-proto = { version = "0.33.0", default-features = false, features = ["gen-tonic-messages", "trace", "logs"] }
prost = "0.14.3"
dekopon-tui = { version = "0.11.1", path = "crates/dekopon-tui" }
serde = { version = "1.0.228", features = ["derive"] }
serde_yaml_ng = "0.10.0"

[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.lints.rustdoc]
broken_intra_doc_links = "deny"

[workspace.lints.clippy]
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
await_holding_invalid_type = "deny"
await_holding_lock = "deny"
await_holding_refcell_ref = "deny"
# Discarding a failure's cause at the site that discards it is the defect class dekopon's own
# review found most often. A site that legitimately drops one takes a scoped `#[allow]` with a
# reason.
map_err_ignore = "deny"
let_underscore_must_use = "deny"

[profile.release]
lto = "thin"
strip = "symbols"
codegen-units = 4
